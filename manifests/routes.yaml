# STAGING ROUTE
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: bookclub-staging-route
  namespace: bookclub-ohtu-s2026
  labels:
    app: bookclub
    type: external
    router: external
spec:
  host: bookclub-ohtu-staging.ext.okd-cs-test-0.k8s.cs.helsinki.fi
  port:
    targetPort: 3003
  to:
    kind: Service
    name: bookclub-staging-svc
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None

---

# PRODUCTION ROUTE
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: bookclub-prod-route
  namespace: bookclub-ohtu-s2026
  labels:
    app: bookclub
    type: external
    router: external
spec:
  host: bookclub-ohtu.ext.okd-cs-test-0.k8s.cs.helsinki.fi
  port:
    targetPort: 3003
  to:
    kind: Service
    name: bookclub-prod-svc
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None